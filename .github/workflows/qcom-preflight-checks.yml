name: Qualcomm Preflight Checks

on:
  pull_request_target:
    branches: [ AudioReach ]
  push:
    branches: [ AudioReach ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@AudioReach
    secrets: inherit
